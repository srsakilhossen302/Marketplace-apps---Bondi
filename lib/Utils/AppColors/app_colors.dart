import 'package:flutter/material.dart';

class AppColors {
  // ==========================================
  // App Background Color
  // ==========================================
  // Deep dark navy background of all screens/pages
  static const Color backgroundColor = Color(0xFF000039);

  // ==========================================
  // Card & Container Colors
  // ==========================================
  // Royal blue background for product listing cards, group cards, list items, and search bars
  static const Color cardColor = Color(0xFF002FA7);

  // ==========================================
  // Active / Selected Colors
  // ==========================================
  // Bright blue for selected category pills, active segment indicators, and highlights
  static const Color selectColor = Color(0xFF0052D4);

  // ==========================================
  // Button Colors
  // ==========================================
  // Bright blue background color for primary actions (e.g., Login, Continue, My Orders buttons)
  static const Color buttonColor = Color(0xFF0052D4);

  // ==========================================
  // Input Field Colors
  // ==========================================
  // Deep blue background fill color for text forms and pin container fields
  static const Color inputFillColor = Color(0xFF0C2B83);

  // ==========================================
  // Accent & Highlight Colors
  // ==========================================
  // Bright cyan/neon blue for prices, verified badges, active icons, and links
  static const Color accentColor = Color(0xFF00E5FF);

  // ==========================================
  // Text Colors
  // ==========================================
  // Solid white for main titles, header text, and button text
  static const Color primaryTextColor = Color(0xFFFFFFFF);

  // Semi-transparent white (approx. 60% opacity) for descriptions, captions, and secondary labels
  static const Color secondaryTextColor = Color(0x99FFFFFF);
}
